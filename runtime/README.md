# Runtime

`runtime/mrr` contains the first AgentOS Minimal Reference Runtime (`MRR v0.1`) codebase.
It is a single-node Rust reference implementation focused on durable `Task + Event + State`
semantics, SQLite-backed persistence, and a small read-only inspector CLI.
